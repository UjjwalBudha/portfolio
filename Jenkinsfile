pipeline {
    agent any

    stages {
        stage('Pre Build') {
            steps {
                // sh 'ls -ltr'
                
            }
        }
        stage ('Build') {
            steps {
            sh 'pwd'
            sh 'touch test.txt'
            sh 'ls -ltr'
            sh 'whoami'
            
            }
        }
    }
}
